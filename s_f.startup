# configuration des IPs et des routes
ip address add 192.168.0.2/24 dev eth0
ip link set dev eth0 up

ip route add default via 192.168.0.1

# resolution des noms pour la connection réseau
cp /shared/resolv.conf /etc/resolv.conf

# pont ssh avec s_admin
apt update
apt install -y openssh-client openssh-server

# mise en place du mot de passe du root
echo "root:root" | chpasswd

# Démarrer SSH
service ssh start

echo "a ecrire pour se connecter sur s_admin : ssh root@192.168.0.3"